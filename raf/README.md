# raf files for simons data

Files with names like "foo.slr" are SLURM scripts, which create output
with names like "foo.raf.gz".

weur.raf.gz is for western Europeans. It includes French and English but
not Basque.